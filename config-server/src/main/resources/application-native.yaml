server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/projects/project 7/digital-bookkeeping-with-microservices/config/cloud/{application}/{profile}